services:
  - type: web
    name: automarket-chat
    runtime: node
    plan: free
    rootDir: chat-server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
